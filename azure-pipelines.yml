# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- task: Kubernetes@1
  inputs:
    connectionType: 'Azure Resource Manager'
    azureSubscriptionEndpoint: '従量課金(e5c1b0a2-8d26-4e8a-a903-8e68ff7a62e3)'
    azureResourceGroup: 'autoTrader'
    kubernetesCluster: 'autoTrader'
    namespace: 'default'
    command: 'apply'
    useConfigurationFile: true
    configuration: 'api-deployment.yaml'
    secretType: 'generic'
- task: Kubernetes@1
  inputs:
    connectionType: 'Azure Resource Manager'
    azureSubscriptionEndpoint: '従量課金(e5c1b0a2-8d26-4e8a-a903-8e68ff7a62e3)'
    azureResourceGroup: 'autoTrader'
    kubernetesCluster: 'autoTrader'
    namespace: 'default'
    command: 'apply'
    useConfigurationFile: true
    configuration: 'api-service.yaml'
    secretType: 'generic'