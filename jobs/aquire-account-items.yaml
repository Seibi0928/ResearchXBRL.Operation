apiVersion: batch/v1
kind: Job
metadata:
  name: aquire-account-items
spec:
  backoffLimit: 6
  completions: 1
  template:
    spec:
      containers:
        - env:
            - name: DB_NAME
              value: postgres
            - name: DB_PASSWORD
              value: mysecretpassword
            - name: DB_PORT
              value: "5432"
            - name: DB_SERVERNAME
              value: rdb-primary
            - name: DB_USERID
              value: postgres
          image: harunari/aquire_account_items_batch:stable
          name: aquire-account-items
      restartPolicy: OnFailure